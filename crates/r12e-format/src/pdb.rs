//! Reading a Windows program database.
//!
//! Placeholder: the reader lands here, feeding the same [`crate::dwarf::DebugInfo`]
//! and [`r12e_types::ctype::Types`] the DWARF reader produces, so everything
//! downstream is told what the compiler knew in one shape rather than two.
